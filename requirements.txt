# bunq Travel Buddy — Python dependencies
# Install: pip install -r requirements.txt
# Requires Python 3.11+

# ── AI / LLM ──────────────────────────────────────────────────────────────────
anthropic>=0.40.0          # Claude visual extractor (extractor.py)

# ── Streamlit app ─────────────────────────────────────────────────────────────
streamlit>=1.35.0          # Web UI — 1.35+ required for st.audio_input

# ── HTTP / FX APIs ────────────────────────────────────────────────────────────
requests>=2.28.0           # fx_converter, geo, bunq_client

# ── bunq auth & request signing ───────────────────────────────────────────────
cryptography>=42.0.0       # RSA keypair generation and signing (bunq_client.py)

# ── AWS audio pipeline ────────────────────────────────────────────────────────
boto3>=1.35.0              # S3 upload, Transcribe, Bedrock (auto_voice_to_eur_aws.py)
sounddevice>=0.4.6         # Microphone recording (CLI mode)
scipy>=1.12.0              # WAV file writing (scipy.io.wavfile)

# ── Environment / secrets ─────────────────────────────────────────────────────
python-dotenv>=1.0.0       # Loads .env file (api_keys.py)

# ── Text-to-speech ────────────────────────────────────────────────────────────
gTTS>=2.5.0                # Google Text-to-Speech (app.py + number_to_output.py)
pygame>=2.5.0              # MP3 playback in the CLI Tkinter UI (number_to_output.py)

# ── Gradio UI template (gui.py — not wired into the main pipeline) ─────────────
gradio>=4.0.0
openai-whisper
opencv-python>=4.8.0
